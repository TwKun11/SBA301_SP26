# Flow Diagram - Login System Architecture

## 🏗️ Component Hierarchy

```
App
├── LoginProvider (wraps entire app)
│   ├── LoginContext.Provider
│   │   └── value: {
│   │       state: { isLoggedIn, username, formUsername, password, error, validated },
│   │       actions: { login, logout, setUsername, setPassword, etc. }
│   │   }
│   │
│   └── Children Components
│       ├── Header (uses useLogin)
│       ├── Login Page (uses useLogin)
│       └── Other Pages
```

## 🔄 State Management Flow

### Login Flow

```
┌─────────────────────┐
│   User enters data  │
│   in Login form     │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ setUsername() / setPassword()       │
│ from useLogin hook                  │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ dispatch({                          │
│   type: LOGIN_ACTIONS.SET_USERNAME, │
│   payload: value                    │
│ })                                  │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ authReducer in LoginProvider        │
│   ├── Handles AUTH actions          │
│   └── Falls through to loginReducer │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ loginReducer (pure function)        │
│   ├── SET_USERNAME                  │
│   ├── SET_PASSWORD                  │
│   ├── SET_ERROR                     │
│   ├── SET_VALIDATED                 │
│   └── RESET_FORM                    │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ New state created immutably         │
│ {                                   │
│   ...state,                         │
│   username: newUsername             │
│ }                                   │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ LoginContext value updated          │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ All components using useLogin()     │
│ are notified of state change        │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ React re-renders affected           │
│ components automatically            │
└─────────────────────────────────────┘
```

### Submit Login Flow

```
┌─────────────────────┐
│ User clicks Login   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ handleSubmit() validates form       │
└──────────┬──────────────────────────┘
           │
           ├── Invalid ──────────────────┐
           │                             │
           │                             ▼
           │                   ┌──────────────────┐
           │                   │ setValidated(true)│
           │                   │ Show errors      │
           │                   └──────────────────┘
           │
           ├── Valid
           │
           ▼
┌─────────────────────────────────────┐
│ login(formUsername, password)       │
│ from useLogin hook                  │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ LoginProvider.login() method        │
│ Checks credentials:                 │
│   admin / 123456                    │
└──────────┬──────────────────────────┘
           │
           ├── Success ──────────────────┐
           │                             │
           │                             ▼
           │                   ┌──────────────────────┐
           │                   │ Save to localStorage │
           │                   │ dispatch LOGIN_SUCCESS│
           │                   │ navigate("/")        │
           │                   └──────────────────────┘
           │
           ├── Failure
           │
           ▼
┌─────────────────────────────────────┐
│ dispatch SET_ERROR                  │
│ Display error message               │
└─────────────────────────────────────┘
```

### Logout Flow

```
┌─────────────────────┐
│ User clicks Logout  │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ logout() from useLogin hook         │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ LoginProvider.logout() method       │
│   ├── Clear localStorage            │
│   └── dispatch({ type: "LOGOUT" })  │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ authReducer handles LOGOUT          │
│ Returns initialAuthState            │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ State reset to:                     │
│ {                                   │
│   isLoggedIn: false,                │
│   username: "",                     │
│   formUsername: "",                 │
│   password: "",                     │
│   error: "",                        │
│   validated: false                  │
│ }                                   │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ All components re-render            │
│ Show logged out state               │
└─────────────────────────────────────┘
```

## 📁 File Dependencies

```
main.jsx
  │
  ├── imports LoginProvider
  │     │
  │     └── from contexts/LoginProvider.jsx
  │           │
  │           ├── imports LoginContext
  │           │     │
  │           │     └── from contexts/LoginContext.jsx
  │           │
  │           └── imports { loginReducer, initialLoginState, LOGIN_ACTIONS }
  │                 │
  │                 └── from store/login/loginReducer.jsx
  │
  └── wraps <App />

Login.jsx / Header.jsx / Any Component
  │
  └── imports useLogin
        │
        └── from hooks/useLogin.js
              │
              └── imports LoginContext
                    │
                    └── from contexts/LoginContext.jsx
```

## 🎭 Hook Usage Pattern

```javascript
// In any component
const {
  // Auth State
  isLoggedIn, // Boolean - đã đăng nhập chưa?
  username, // String - tên user đã login

  // Form State
  formUsername, // String - username trong form
  password, // String - password trong form
  error, // String - thông báo lỗi
  validated, // Boolean - form đã validate?

  // Auth Actions
  login, // (username, password) => { success, error? }
  logout, // () => void

  // Form Actions
  setUsername, // (value) => void
  setPassword, // (value) => void
  setValidated, // (boolean) => void
  setError, // (message) => void
  resetForm, // () => void
} = useLogin();
```

## 🔐 Session Persistence

```
Page Load
    ↓
LoginProvider useEffect runs
    ↓
Check localStorage
    ├── Has session? ────────────┐
    │                            │
    │                            ▼
    │                   ┌──────────────────────┐
    │                   │ dispatch             │
    │                   │ RESTORE_SESSION      │
    │                   │ Set isLoggedIn=true  │
    │                   └──────────────────────┘
    │
    └── No session? ────────────┐
                                │
                                ▼
                       ┌──────────────────────┐
                       │ Keep initial state   │
                       │ isLoggedIn=false     │
                       └──────────────────────┘
```

## 🎯 Benefits Visualization

```
┌─────────────────────────────────────────────────────┐
│            WITHOUT useContext + useReducer          │
├─────────────────────────────────────────────────────┤
│  App                                                │
│   ├── [isLoggedIn, username] ─── props ───┐        │
│   │                                         │        │
│   ├── Header [receives props] ←────────────┘        │
│   │                                                  │
│   ├── Main                                          │
│   │   └── [passes props down] ─── props ───┐       │
│   │                                          │       │
│   └── Footer [receives props] ←─────────────┘       │
│                                                      │
│  ❌ Props drilling                                  │
│  ❌ State in multiple places                        │
│  ❌ Hard to maintain                                │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│             WITH useContext + useReducer            │
├─────────────────────────────────────────────────────┤
│  LoginProvider                                      │
│   └── [Global State]                                │
│        ↓         ↓         ↓                        │
│     Header    Main     Footer                       │
│     useLogin() useLogin() useLogin()                │
│                                                      │
│  ✅ No props drilling                               │
│  ✅ Single source of truth                          │
│  ✅ Easy to maintain                                │
│  ✅ Reusable logic                                  │
└─────────────────────────────────────────────────────┘
```
